<project name="NLPStanfordXMLServer">

    <property name="main-class"  value="NLPStanfordXMLServer"/>
    <property name="src.dir"     value="src"/>
    <property name="build.dir"   value="build"/>
    <property name="classes.dir" value="${build.dir}/classes"/>
    <property name="jar.dir"     value="${build.dir}/jar"/>
    <property name="lib.dir"     value="lib"/>
    <property name="port"  value=""/>

    <path id="classpath">
        <fileset dir="${lib.dir}" includes="**/*.jar"/>
    </path>

    <target name="clear" description="Delete all generated files...">
        <delete dir="${build.dir}"/>
    </target>

    <target name="libs" depends="simple, stanford" description="Download and compile 3rd party libraries..." />

    <target name="simple">
        <get src="https://downloads.sourceforge.net/project/simpleweb/simpleweb/5.1.6/simple-5.1.6.zip" dest="simple-5.1.6.zip" />
        <unzip src="simple-5.1.6.zip" dest="." />
        <ant dir="simple-5.1.6" target="build" />
        <move file="simple-5.1.6/jar/simple-5.1.6.jar" todir="${lib.dir}" />
        <delete file="simple-5.1.6.zip" />
        <delete dir="simple-5.1.6" />
    </target>

    <target name="stanford">
        <get src="https://nlp.stanford.edu/software/stanford-corenlp-4.0.0.zip" dest="stanford-corenlp-4.0.0.zip" />
        <unzip src="stanford-corenlp-4.0.0.zip" dest="." />
        <move todir="${lib.dir}">
            <fileset dir="stanford-corenlp-4.0.0">
                <include name="**/*.jar" />
            </fileset>
        </move>
        <delete dir="stanford-corenlp-4.0.0" />
        <delete file="stanford-corenlp-full-2020-04-20.zip" />
    </target>

    <target name="compile">
        <mkdir dir="${classes.dir}"/>
        <javac srcdir="${src.dir}" destdir="${classes.dir}" includeantruntime="false" classpathref="classpath" />
    </target>

    <target name="jar" depends="compile">
        <mkdir dir="${jar.dir}"/>
        <jar destfile="${jar.dir}/${ant.project.name}.jar" basedir="${classes.dir}">
            <manifest>
                <attribute name="Main-Class" value="${main-class}"/>
            </manifest>
            <zipgroupfileset dir="${lib.dir}" includes="**/*.jar"/>
        </jar>
    </target>

    <target name="run">
        <java jar="${jar.dir}/${ant.project.name}.jar" fork="true" maxmemory="512m">
            <arg value="${port}" />
        </java>
    </target>

    <target name="test">
        <java jar="${jar.dir}/${ant.project.name}.jar" fork="true" timeout="30000" maxmemory="512m">
            <arg value="${port}" />
        </java>
    </target>
</project>
